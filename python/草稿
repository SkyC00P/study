VS code 如何调试使用Python
https://code.visualstudio.com/docs/python/python-tutorial

Python 的异常处理机制是怎么样的？
Python 的模块导入机制是怎么样的？

Python None 值是否跟 Java 的Null值一样呢？
Python 变量作用域两个关键字 nonlocal 和 global

Python 字典，列表，元祖里包含中文字符的打印情况
https://www.cnblogs.com/pyxiaomangshe/p/7864903.html

Python 的依赖包离线安装方式有两种 setup , whl

Python yield 的用法

Python 的数据库连接中的动态查询类型有以下几种
qmark
    Question mark style, e.g. '...WHERE name=?'
numeric
    Numeric, positional style, e.g. '...WHERE name=:1'
named
    Named style, e.g. '...WHERE name=:name'
format
    ANSI C printf format codes, e.g. "...WHERE name=%s'
pyformat
    Python extended format codes, e.g. '...WHERE name=%(name)s

Python 查询时如何让返回的结果转化为对象或字典

Python 好用的ORM框架 SQLObject, SQLAlchemy